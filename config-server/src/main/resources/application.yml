server:
  port: ${PORT:8888}
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/angelestrada14019/microservice-catalog-movies
          search-paths: config-data
          default-label: master
